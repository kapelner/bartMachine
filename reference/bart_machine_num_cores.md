# Get Number of Cores Used by BART

Returns number of cores used by BART

## Usage

``` r
bart_machine_num_cores()
```

## Value

Number of cores currently being used by parallelized BART functions.

## Details

Returns the number of cores currently being used by parallelized BART
functions

## See also

[`set_bart_machine_num_cores`](https://kapelner.github.io/bartMachine/reference/set_bart_machine_num_cores.md)

## Author

Adam Kapelner and Justin Bleich

## Examples

``` r
if (FALSE) { # \dontrun{
bart_machine_num_cores()
} # }
```
